# Rabbit Hole AI 创作工具集 - 产品技术设计文档

> **版本**: v1.0  
> **日期**: 2026-01-22  
> **状态**: 设计确认

---

## 一、产品概述

### 1.1 产品定位

**Rabbit Hole** 是 HoppingRabbit AI 的 AI 创作工具集，专为**口播用户**打造。参考 Higgsfield AI 的产品形态，提供独立的 AI 创作入口，生成的内容无缝衔接到视频编辑器。

### 1.2 核心价值

```
┌─────────────────────────────────────────────────────────────────────┐
│                        用户价值主张                                  │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│   🎭 数字人口播      一段音频 → 多个数字人形象 → 批量口播视频        │
│                                                                     │
│   🖼️ 产品动态化     静态产品图 → 动态展示视频 → 带货素材            │
│                                                                     │
│   🔄 内容复用       一条口播 → 换脸/换背景 → 多版本分发              │
│                                                                     │
│   ✂️ 无缝剪辑       AI生成 → 素材库 → 编辑器 → 精细调整              │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

### 1.3 与竞品差异

| 特性 | Higgsfield AI | HoppingRabbit (我们) |
|------|--------------|---------------------|
| 定位 | 通用 AI 视频创作 | **口播场景专精** |
| 生成结果 | 下载/分享 | **直接进入编辑器剪辑** |
| 编辑能力 | 无 | **全功能时间线编辑器** |
| 特效方向 | 大特效、电影级 VFX | **口播实用特效**（换背景、换人） |
| 工作流 | 独立工具 | **AI生成 + 剪辑一体化** |

---

## 二、整体架构

### 2.1 用户旅程

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                            用户旅程 / User Journey                          │
└─────────────────────────────────────────────────────────────────────────────┘

     ┌──────────────┐         ┌──────────────┐         ┌──────────────┐
     │  Workspace   │         │ Rabbit Hole  │         │   Editor     │
     │   工作台入口  │ ──────▶ │  AI创作工具集 │ ──────▶ │  视频编辑器  │
     └──────────────┘         └──────────────┘         └──────────────┘
            │                        │                        │
            │                        ▼                        ▼
            │               ┌────────────────┐      ┌────────────────┐
            │               │  AI 创作工具   │      │  Clip级AI工具  │
            │               │ ─────────────  │      │ ─────────────  │
            │               │ • 数字人口播   │      │ • 口型同步     │
            │               │ • 图生视频     │      │ • AI换脸       │
            │               │ • 文生视频     │      │ • 换背景       │
            │               │ • AI换脸       │      │ • 画质增强     │
            │               │ • 换背景       │      └────────┬───────┘
            │               │ • 批量生成     │               │
            │               └────────┬───────┘               │
            │                        │                       │
            │                        ▼                       │
            │               ┌────────────────┐               │
            │               │   我的素材     │◀──────────────┘
            │               │  (Assets)      │
            │               └────────┬───────┘
            │                        │
            ▼                        ▼
     ┌──────────────────────────────────────────────────────────────┐
     │                    Timeline / 时间线                         │
     │  ┌─────┐ ┌─────┐ ┌─────┐ ┌─────┐ ┌─────┐                    │
     │  │Clip │ │Clip │ │Clip │ │Clip │ │Clip │                    │
     │  │(AI) │ │(上传)│ │(AI) │ │(AI) │ │(AI) │                    │
     │  └─────┘ └─────┘ └─────┘ └─────┘ └─────┘                    │
     └────────────────────────────┬─────────────────────────────────┘
                                  │
                                  ▼
                         ┌────────────────┐
                         │   导出成片      │
                         │   Export        │
                         └────────────────┘
```

### 2.2 系统架构

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              Frontend (Next.js)                             │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │                    Workspace / 工作台                                │   │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐            │   │
│  │  │   Home   │  │  Videos  │  │  Rabbit  │  │ Exports  │            │   │
│  │  │   首页   │  │  素材库  │  │   Hole   │  │  导出    │            │   │
│  │  └──────────┘  └──────────┘  └──────────┘  └──────────┘            │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │                    Editor / 视频编辑器                               │   │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐            │   │
│  │  │ Timeline │  │  Assets  │  │ AI Tools │  │ Preview  │            │   │
│  │  │  时间线  │  │  素材库  │  │  AI工具  │  │  预览    │            │   │
│  │  └──────────┘  └──────────┘  └──────────┘  └──────────┘            │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
└──────────────────────────────────┬──────────────────────────────────────────┘
                                   │ REST API
                                   ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                              Backend (FastAPI)                              │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐   │
│  │  /api/kling  │  │ /api/assets  │  │ /api/projects│  │ /api/export  │   │
│  │  AI能力路由  │  │  素材管理    │  │  项目管理    │  │  导出服务    │   │
│  └──────┬───────┘  └──────────────┘  └──────────────┘  └──────────────┘   │
│         │                                                                   │
│  ┌──────▼───────────────────────────────────────────────────────────────┐  │
│  │                    Services / 服务层                                  │  │
│  │  ┌────────────────┐  ┌────────────────┐  ┌────────────────┐         │  │
│  │  │ KlingAIClient  │  │ KouboService   │  │ AssetService   │         │  │
│  │  │  可灵AI客户端  │  │  口播场景服务   │  │  素材服务      │         │  │
│  │  └────────────────┘  └────────────────┘  └────────────────┘         │  │
│  └──────────────────────────────────────────────────────────────────────┘  │
│                                   │                                         │
│  ┌────────────────────────────────▼─────────────────────────────────────┐  │
│  │                    Celery / 异步任务队列                              │  │
│  │  长时间 AI 任务后台执行 + WebSocket 进度推送                          │  │
│  └──────────────────────────────────────────────────────────────────────┘  │
│                                                                             │
└──────────────────────────────────┬──────────────────────────────────────────┘
                                   │ HTTPS
                                   ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                           可灵AI API (Kling AI)                             │
│                       https://api.klingai.com/v1                            │
├─────────────────────────────────────────────────────────────────────────────┤
│  /lipsync/create      口型同步                                              │
│  /video/text2video    文生视频                                              │
│  /video/image2video   图生视频                                              │
│  /faceswap/create     AI换脸                                                │
│  /segment/create      背景分离/替换                                         │
│  /task/{id}           任务状态查询                                          │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 三、功能模块设计

### 3.1 Rabbit Hole 工具集

#### 工具分类

```
Rabbit Hole/
│
├── 🔥 核心功能 (Core) - Phase 1 MVP
│   │
│   └── 口型同步 ⭐ P0 (MVP 首发)
│       ├── 输入: 用户上传的口播视频 + 新音频
│       ├── 输出: 口型同步的新视频
│       └── 流程: 上传视频 → 上传音频 → 生成 → 存素材
│
├── 🎭 数字人口播 (Phase 2 - 暂缓)
│   │
│   ├── 数字人形象库
│   │   ├── 输入: 口播音频 + 数字人形象
│   │   ├── 输出: 口型同步的口播视频
│   │   └── 流程: 音频上传 → 选择形象 → [选背景] → 生成 → 存素材
│   │
│   └── 产品动态展示
│       ├── 输入: 产品图片列表
│       ├── 输出: 动态展示视频片段
│       └── 流程: 图片上传 → 选择动效 → 批量生成 → 存素材
│
├── 🔄 AI 换脸 (Phase 2)
│   │
│   └── AI 换脸
│       ├── 输入: 源视频 + 目标人脸图片
│       ├── 输出: 换脸后的视频
│       └── 流程: 视频选择 → 人脸上传 → 生成 → 存素材
│
├── 🎬 视频生成 (Generation)
│   │
│   ├── 文生视频 P1
│   │   ├── 输入: 文字描述 + 风格选择
│   │   ├── 输出: AI 生成的视频
│   │   └── 用途: 口播背景、B-roll 素材
│   │
│   └── 图生视频 P1
│       ├── 输入: 静态图片 + 运动描述
│       ├── 输出: 动态视频
│       └── 用途: 产品展示、封面动效
│
├── 👤 人物处理 (Character)
│   │
│   ├── 口型同步 P0
│   │   ├── 输入: 视频 + 新音频
│   │   ├── 输出: 口型同步的视频
│   │   └── 用途: 多语言配音、音频替换
│   │
│   └── 换背景 P1
│       ├── 输入: 口播视频 + 背景图/视频
│       ├── 输出: 换背景后的视频
│       └── 用途: 场景切换、品牌定制
│
├── 📦 批量工具 (Batch)
│   │
│   ├── 批量换脸 P2
│   │   ├── 输入: 源视频 + 多个人脸
│   │   ├── 输出: 多个换脸版本
│   │   └── 用途: A/B 测试、多平台分发
│   │
│   └── 批量换背景 P2
│       ├── 输入: 口播视频 + 多个背景
│       ├── 输出: 多个背景版本
│       └── 用途: 场景多样化
│
└── 🗂️ 形象库 (Avatar Library)
    │
    ├── 预设形象
    │   └── 系统预置的数字人口播视频模板
    │
    └── 我的形象
        └── 用户上传自己的口播视频作为模板（无需训练）
```

#### 功能优先级

| 优先级 | 功能 | 阶段 | 说明 |
|--------|------|------|------|
| **P0** | 口型同步 | **Phase 1 MVP** | 用户上传视频 + 音频 → 口型同步 |
| P1 | 数字人形象库 | Phase 2 | 预设形象 + 音频 → 口播视频 |
| P1 | 产品动态展示 | Phase 2 | 带货场景，图片 → 动态视频 |
| P1 | AI 换脸 | Phase 2 | 批量分身，换形象 |
| P1 | 换背景 | Phase 2 | 场景切换 |
| P1 | 文/图生视频 | Phase 2 | B-roll 素材生成 |
| P2 | 批量工具 | Phase 3 | 效率工具 |

### 3.2 Editor AI Tools (Clip 级工具)

在编辑器中选中 Clip 后，可应用的 AI 能力：

```
┌─────────────────────────────────────────────────────────────────┐
│               Editor AI Tools / 编辑器 AI 工具                  │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  触发方式:                                                       │
│  1. 选中 Clip → 工具栏「AI 工具」按钮                            │
│  2. 选中 Clip → 右键菜单 → AI 增强                               │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  👤 人物处理                                             │   │
│  │  ├── 口型同步    替换音频，重新同步口型                   │   │
│  │  ├── AI 换脸     换成其他人物形象                         │   │
│  │  └── 换背景      保留人物，更换背景                       │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  🎨 画面处理                                             │   │
│  │  ├── 画质增强    超分辨率、降噪                           │   │
│  │  └── 智能调色    自动色彩校正                             │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  处理流程:                                                       │
│  选中 Clip → 选择 AI 工具 → 配置参数 → 提交任务                  │
│           → 后台处理 → 生成新 Asset → 替换/新增 Clip             │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 3.3 数字人形象库

> **设计原则**: 初版不做形象训练，直接用「口型同步」技术，让预设/用户上传的视频匹配新音频。

```
┌─────────────────────────────────────────────────────────────────┐
│                    数字人形象库 / Avatar Library                │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  工作原理:                                                       │
│  用户选择一个「口播视频模板」+ 上传新音频 → AI 口型同步 → 新视频  │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  📦 预设形象 (System Presets)                            │   │
│  │                                                          │   │
│  │  平台提供的口播视频模板，用户直接选用                      │   │
│  │                                                          │   │
│  │  ┌────────┐ ┌────────┐ ┌────────┐ ┌────────┐           │   │
│  │  │ 商务男 │ │ 商务女 │ │ 时尚女 │ │ 阳光男 │           │   │
│  │  │ 🎬     │ │ 🎬     │ │ 🎬     │ │ 🎬     │           │   │
│  │  └────────┘ └────────┘ └────────┘ └────────┘           │   │
│  │                                                          │   │
│  │  ┌────────┐ ┌────────┐ ┌────────┐ ┌────────┐           │   │
│  │  │ 甜美女 │ │ 主播男 │ │ 知性女 │ │ 更多...│           │   │
│  │  │ 🎬     │ │ 🎬     │ │ 🎬     │ │ 🎬     │           │   │
│  │  └────────┘ └────────┘ └────────┘ └────────┘           │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  👤 我的形象 (My Avatars)                                │   │
│  │                                                          │   │
│  │  用户上传自己的口播视频作为模板，后续复用                  │   │
│  │                                                          │   │
│  │  上传要求:                                                │   │
│  │  • 正脸清晰可见的口播视频                                 │   │
│  │  • 光线均匀，嘴部无遮挡                                   │   │
│  │  • 建议时长 10-60 秒                                      │   │
│  │  • 说话节奏自然                                           │   │
│  │                                                          │   │
│  │  使用流程 (无需训练，即传即用):                           │   │
│  │  上传口播视频 → 保存为「我的形象」→ 选择形象 + 新音频     │   │
│  │              → 口型同步生成 → 新口播视频                  │   │
│  │                                                          │   │
│  │  ┌────────┐ ┌────────┐ ┌────────┐                       │   │
│  │  │ 我的1  │ │ 我的2  │ │   +    │                       │   │
│  │  │ 🎬     │ │ 🎬     │ │  上传  │                       │   │
│  │  └────────┘ └────────┘ └────────┘                       │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

#### 技术实现说明

| 方案 | 原理 | 成本 | 效果 |
|------|------|------|------|
| **口型同步** | 可灵AI 的 `/lipsync/create` 接口 | ~¥0.1-0.5/秒 | 嘴型匹配新音频 |
| ~~形象训练~~ | ~~AI 学习用户特征~~ | ~~¥500+~~ | ~~初版不做~~ |

---

## 四、数据流设计

### 4.1 Rabbit Hole 生成 → 素材库 → 编辑器

```
┌──────────────────────────────────────────────────────────────────────────┐
│                    数据流: AI 生成 → 素材库 → 编辑器                      │
└──────────────────────────────────────────────────────────────────────────┘

Step 1: 用户在 Rabbit Hole 发起 AI 创作
┌───────────────┐
│ 用户输入       │  音频文件 / 图片 / 视频 / 文本描述
│ (Raw Input)   │
└───────┬───────┘
        │
        ▼
┌───────────────┐
│ 创建 AI 任务  │  POST /api/kling/koubo/digital-human
│ (Create Task) │  返回 task_id
└───────┬───────┘
        │
        ▼
Step 2: 后台异步处理
┌───────────────┐     ┌───────────────┐     ┌───────────────┐
│ Celery Worker │────▶│  可灵AI API   │────▶│  轮询状态     │
│ (异步任务)     │     │  (处理中)     │     │  (进度更新)   │
└───────────────┘     └───────────────┘     └───────┬───────┘
                                                    │
                                                    ▼
Step 3: 处理完成，存入素材库
┌───────────────┐     ┌───────────────┐     ┌───────────────┐
│ 下载视频到    │────▶│ 上传到        │────▶│ 创建 Asset    │
│ 临时目录      │     │ Supabase      │     │ 记录          │
└───────────────┘     │ Storage       │     └───────┬───────┘
                      └───────────────┘             │
                                                    │
                                                    ▼
Step 4: 通知前端，用户查看
┌───────────────┐     ┌───────────────┐     ┌───────────────┐
│ WebSocket     │────▶│ 前端更新      │────▶│ 显示在        │
│ 推送完成通知  │     │ 任务状态      │     │ "我的素材"    │
└───────────────┘     └───────────────┘     └───────┬───────┘
                                                    │
                                                    ▼
Step 5: 用户将素材添加到编辑器
┌───────────────┐     ┌───────────────┐     ┌───────────────┐
│ 用户选择      │────▶│ 打开编辑器    │────▶│ 拖入时间线    │
│ "在编辑器打开"│     │ 自动关联Asset │     │ 创建 Clip     │
└───────────────┘     └───────────────┘     └───────────────┘
```

### 4.2 Editor AI Tools 数据流

```
┌──────────────────────────────────────────────────────────────────────────┐
│                    数据流: Clip → AI 增强 → 新 Clip                       │
└──────────────────────────────────────────────────────────────────────────┘

Step 1: 用户选中 Clip，应用 AI 工具
┌───────────────┐
│ 选中 Clip     │  clip_id, asset_url
│ (Video Clip)  │
└───────┬───────┘
        │
        ▼
┌───────────────┐
│ 打开 AI 工具  │  选择: 口型同步 / 换脸 / 换背景
│ (AI Tools)    │  配置参数 (如: 上传新音频)
└───────┬───────┘
        │
        ▼
Step 2: 提交 AI 处理任务
┌───────────────┐
│ 创建任务      │  POST /api/kling/clip/{clip_id}/lip-sync
│ source_clip_id│  记录源 Clip 关联
└───────┬───────┘
        │
        ▼
Step 3: 后台处理 (同 Rabbit Hole)
┌───────────────┐     ┌───────────────┐
│ 可灵AI 处理   │────▶│ 生成新视频    │
│ (异步)        │     │ (output_url)  │
└───────────────┘     └───────┬───────┘
                              │
                              ▼
Step 4: 创建新 Asset，更新 Clip
┌───────────────┐     ┌───────────────┐     ┌───────────────┐
│ 创建新 Asset  │────▶│ 创建新 Clip   │────▶│ 时间线更新    │
│ (AI 生成)     │     │ 或替换原 Clip │     │ (保持位置)    │
└───────────────┘     └───────────────┘     └───────────────┘

Note: 原 Clip 的 Asset 保留在素材库，用户可随时切换回原版本
```

---

## 五、数据库设计

### 5.1 新增表结构

```sql
-- =============================================
-- AI 任务表
-- =============================================
CREATE TABLE ai_tasks (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID NOT NULL REFERENCES auth.users(id) ON DELETE CASCADE,
  project_id UUID REFERENCES projects(id) ON DELETE SET NULL,
  
  -- 任务类型
  task_type TEXT NOT NULL,
  -- 可选值: digital_human, lip_sync, face_swap, background_replace,
  --        text_to_video, image_to_video, batch_face_swap, batch_background
  
  -- 任务来源
  source TEXT NOT NULL DEFAULT 'rabbit_hole',
  -- 可选值: rabbit_hole (Rabbit Hole 创作), editor (编辑器 AI 工具)
  
  -- 关联的源 Clip (仅 editor 来源)
  source_clip_id UUID REFERENCES clips(id) ON DELETE SET NULL,
  
  -- 输入参数 (JSON)
  input_params JSONB NOT NULL DEFAULT '{}',
  -- 示例:
  -- digital_human: {"audio_url": "...", "avatar_id": "...", "background_url": "..."}
  -- lip_sync: {"video_url": "...", "audio_url": "..."}
  -- face_swap: {"video_url": "...", "face_image_url": "..."}
  
  -- 第三方 AI 服务
  provider TEXT NOT NULL DEFAULT 'kling',
  provider_task_id TEXT,  -- 可灵AI 返回的 task_id
  
  -- 状态
  status TEXT NOT NULL DEFAULT 'pending',
  -- 可选值: pending, processing, completed, failed, cancelled
  progress INTEGER DEFAULT 0,  -- 0-100
  status_message TEXT,  -- 当前状态描述
  
  -- 输出
  output_url TEXT,  -- 生成的视频 URL (可灵返回)
  output_asset_id UUID REFERENCES assets(id) ON DELETE SET NULL,  -- 关联到素材库
  
  -- 错误信息
  error_code TEXT,
  error_message TEXT,
  
  -- 时间戳
  created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  started_at TIMESTAMPTZ,
  completed_at TIMESTAMPTZ,
  
  -- 索引
  CONSTRAINT ai_tasks_status_check CHECK (
    status IN ('pending', 'processing', 'completed', 'failed', 'cancelled')
  )
);

-- 索引
CREATE INDEX idx_ai_tasks_user_id ON ai_tasks(user_id);
CREATE INDEX idx_ai_tasks_status ON ai_tasks(status);
CREATE INDEX idx_ai_tasks_created_at ON ai_tasks(created_at DESC);

-- =============================================
-- 数字人形象库 (口播视频模板)
-- =============================================
CREATE TABLE avatars (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID REFERENCES auth.users(id) ON DELETE CASCADE,  -- NULL = 系统预设
  
  -- 基本信息
  name TEXT NOT NULL,
  description TEXT,
  thumbnail_url TEXT,  -- 缩略图 (视频首帧)
  
  -- 类型
  type TEXT NOT NULL DEFAULT 'preset',
  -- 可选值: preset (系统预设), user (用户上传)
  
  -- 视频资源
  video_url TEXT NOT NULL,       -- 口播视频模板 URL
  video_duration FLOAT,          -- 视频时长(秒)
  
  -- 标签
  tags TEXT[] DEFAULT '{}',  -- 如: ["商务", "男性", "中年"]
  
  -- 元数据
  metadata JSONB DEFAULT '{}',
  -- 示例: {"resolution": "1920x1080", "fps": 30}
  
  -- 状态
  is_active BOOLEAN DEFAULT TRUE,
  
  -- 时间戳
  created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

-- 索引
CREATE INDEX idx_avatars_user_id ON avatars(user_id);
CREATE INDEX idx_avatars_type ON avatars(type);
CREATE INDEX idx_avatars_is_active ON avatars(is_active);

-- 预设形象数据
INSERT INTO avatars (name, description, type, source_type, source_url, tags, thumbnail_url) VALUES
('商务男士', '专业商务风格男性形象', 'preset', 'video', 'https://...', ARRAY['商务', '男性'], 'https://...'),
('商务女士', '专业商务风格女性形象', 'preset', 'video', 'https://...', ARRAY['商务', '女性'], 'https://...'),
('时尚达人', '时尚潮流女性形象', 'preset', 'video', 'https://...', ARRAY['时尚', '女性'], 'https://...'),
('阳光男孩', '活力阳光男性形象', 'preset', 'video', 'https://...', ARRAY['活力', '男性'], 'https://...');

-- =============================================
-- 扩展 assets 表
-- =============================================
ALTER TABLE assets ADD COLUMN IF NOT EXISTS ai_task_id UUID REFERENCES ai_tasks(id) ON DELETE SET NULL;
ALTER TABLE assets ADD COLUMN IF NOT EXISTS ai_generated BOOLEAN DEFAULT FALSE;

-- =============================================
-- 扩展 clips 表
-- =============================================
ALTER TABLE clips ADD COLUMN IF NOT EXISTS ai_source_task_id UUID REFERENCES ai_tasks(id) ON DELETE SET NULL;
ALTER TABLE clips ADD COLUMN IF NOT EXISTS ai_enhanced BOOLEAN DEFAULT FALSE;
ALTER TABLE clips ADD COLUMN IF NOT EXISTS original_clip_id UUID REFERENCES clips(id) ON DELETE SET NULL;
-- original_clip_id: 如果是 AI 增强生成的 Clip，指向原始 Clip
```

### 5.2 数据关系

```
┌─────────────────────────────────────────────────────────────────┐
│                      数据关系图 / ER Diagram                     │
└─────────────────────────────────────────────────────────────────┘

  ┌──────────┐         ┌──────────┐         ┌──────────┐
  │  users   │────────▶│ projects │────────▶│  clips   │
  └──────────┘         └──────────┘         └──────────┘
       │                    │                    │ ▲
       │                    │                    │ │ original_clip_id
       │                    │                    │ │ (AI增强后指向原Clip)
       │                    │                    │ │
       ▼                    ▼                    ▼ │
  ┌──────────┐         ┌──────────┐         ┌──────────┐
  │ ai_tasks │────────▶│  assets  │◀────────│ ai_tasks │
  └──────────┘         └──────────┘         └──────────┘
       │                    ▲                    │
       │                    │                    │
       │         output_asset_id                 │
       │                    │                    │
       └────────────────────┘                    │
                                                 │
  ┌──────────┐                                   │
  │ avatars  │◀──────────────────────────────────┘
  └──────────┘   (digital_human 任务引用)
```

---

## 六、API 设计

### 6.1 Rabbit Hole APIs

```yaml
# =============================================
# 数字人口播
# =============================================
POST /api/kling/koubo/digital-human
Request:
  audio_url: string          # 口播音频 URL (必填)
  avatar_id: string          # 数字人形象 ID (必填)
  background_url?: string    # 背景图/视频 URL (可选)
  background_prompt?: string # 背景生成提示词 (可选)
Response:
  task_id: string
  status: "pending"

# =============================================
# 产品动态展示
# =============================================
POST /api/kling/koubo/product-showcase
Request:
  product_images: string[]   # 产品图片 URL 列表
  motion_style?: string      # 动效风格: zoom_in, pan, rotate
  duration?: number          # 每张图的时长 (秒)
Response:
  task_id: string
  status: "pending"

# =============================================
# 文生视频
# =============================================
POST /api/kling/text-to-video
Request:
  prompt: string             # 正向提示词
  negative_prompt?: string   # 负向提示词
  duration?: number          # 时长: 5 或 10 秒
  aspect_ratio?: string      # 宽高比: 16:9, 9:16, 1:1
  style?: string             # 风格: realistic, anime
Response:
  task_id: string
  status: "pending"

# =============================================
# 图生视频
# =============================================
POST /api/kling/image-to-video
Request:
  image_url: string          # 源图片 URL
  prompt?: string            # 运动描述
  duration?: number          # 时长
  motion_scale?: number      # 运动幅度 0.5-1.5
Response:
  task_id: string
  status: "pending"

# =============================================
# AI 换脸
# =============================================
POST /api/kling/face-swap
Request:
  video_url: string          # 源视频 URL
  face_image_url: string     # 目标人脸图片 URL
  face_index?: number        # 视频中第几张脸
Response:
  task_id: string
  status: "pending"

# =============================================
# 换背景
# =============================================
POST /api/kling/background-replace
Request:
  video_url: string          # 源视频 URL (含人物)
  background_url?: string    # 新背景图/视频 URL
  background_prompt?: string # 或用 AI 生成背景
Response:
  task_id: string
  status: "pending"

# =============================================
# 批量换脸
# =============================================
POST /api/kling/koubo/batch-avatars
Request:
  source_video_url: string   # 源视频
  face_images: string[]      # 多个目标人脸
Response:
  task_id: string
  sub_tasks: string[]        # 子任务 ID 列表
  status: "pending"
```

### 6.2 Editor AI Tools APIs

```yaml
# =============================================
# 对 Clip 应用口型同步
# =============================================
POST /api/kling/clip/{clip_id}/lip-sync
Request:
  audio_url: string          # 新音频 URL
  replace_original?: boolean # 是否替换原 Clip (默认 false)
Response:
  task_id: string
  status: "pending"

# =============================================
# 对 Clip 应用换脸
# =============================================
POST /api/kling/clip/{clip_id}/face-swap
Request:
  face_image_url: string     # 目标人脸
  replace_original?: boolean
Response:
  task_id: string
  status: "pending"

# =============================================
# 对 Clip 应用换背景
# =============================================
POST /api/kling/clip/{clip_id}/background-replace
Request:
  background_url?: string
  background_prompt?: string
  replace_original?: boolean
Response:
  task_id: string
  status: "pending"
```

### 6.3 通用 APIs

```yaml
# =============================================
# 查询任务状态
# =============================================
GET /api/kling/task/{task_id}
Response:
  task_id: string
  task_type: string
  status: "pending" | "processing" | "completed" | "failed"
  progress: number           # 0-100
  status_message?: string
  output_url?: string        # 完成后的视频 URL
  output_asset_id?: string   # 关联的 Asset ID
  error?: string

# =============================================
# 取消任务
# =============================================
POST /api/kling/task/{task_id}/cancel
Response:
  success: boolean

# =============================================
# 获取用户的 AI 任务列表
# =============================================
GET /api/kling/tasks
Query:
  status?: string            # 筛选状态
  task_type?: string         # 筛选类型
  page?: number
  page_size?: number
Response:
  tasks: Task[]
  total: number

# =============================================
# 获取能力列表
# =============================================
GET /api/kling/capabilities
Response:
  capabilities: Capability[]
  workflows: Workflow[]

# =============================================
# 形象库 - 获取列表
# =============================================
GET /api/avatars
Query:
  type?: "preset" | "custom"
  tags?: string[]
Response:
  avatars: Avatar[]

# =============================================
# 形象库 - 上传口播视频模板
# =============================================
POST /api/avatars
Request:
  name: string
  file: File                     # 口播视频文件
Response:
  avatar_id: string
  video_url: string
  thumbnail_url: string
  status: "ready"                # 无需训练，立即可用
```

---

## 七、前端设计

### 7.1 页面结构

```
frontend/src/
│
├── app/
│   └── workspace/
│       └── page.tsx                    # Workspace 主页面
│
├── components/
│   └── workspace/
│       ├── RabbitHoleView.tsx          # ★ Rabbit Hole 主视图
│       ├── RabbitHoleToolCard.tsx      # 工具卡片
│       ├── AvatarLibrary.tsx           # 形象库
│       ├── AvatarUploader.tsx          # 形象上传
│       └── AITaskProgress.tsx          # 任务进度组件
│
├── features/
│   └── editor/
│       └── components/
│           ├── AIToolsPanel.tsx        # ★ 扩展 AI 工具面板
│           ├── ClipAIMenu.tsx          # Clip 右键 AI 菜单
│           ├── LipSyncDialog.tsx       # 口型同步对话框
│           ├── FaceSwapDialog.tsx      # 换脸对话框
│           └── BackgroundReplaceDialog.tsx  # 换背景对话框
│
└── lib/
    └── kling-api.ts                    # 可灵 API 客户端
```

### 7.2 Rabbit Hole 界面设计

```
┌─────────────────────────────────────────────────────────────────────────────┐
│  ◀ HoppingRabbit                                            用户头像 ▼     │
├───────┬─────────────────────────────────────────────────────────────────────┤
│       │                                                                     │
│  🏠   │  ┌─────────────────────────────────────────────────────────────┐   │
│  Home │  │  🐰 Rabbit Hole                        AI 创作工具集        │   │
│       │  └─────────────────────────────────────────────────────────────┘   │
│  📁   │                                                                     │
│  素材 │  ┌─────────────────────────────────────────────────────────────┐   │
│       │  │  🔥 核心功能                                                 │   │
│  🐰   │  │                                                              │   │
│ Rabbit│  │  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │   │
│  Hole │  │  │  🎭 数字人   │  │  🖼️ 产品展示 │  │  🔄 AI换脸  │      │   │
│  ◀──  │  │  │    口播      │  │     动态化    │  │              │      │   │
│       │  │  │              │  │              │  │              │      │   │
│  📤   │  │  │ 音频+形象→  │  │ 产品图→动态 │  │ 换形象版本  │      │   │
│  导出 │  │  │ 口播视频    │  │ 展示视频    │  │              │      │   │
│       │  │  │              │  │              │  │              │      │   │
│  🗑️   │  │  │  开始创作 → │  │  开始创作 → │  │  开始创作 → │      │   │
│  回收 │  │  └──────────────┘  └──────────────┘  └──────────────┘      │   │
│       │  │                                                              │   │
│       │  └─────────────────────────────────────────────────────────────┘   │
│       │                                                                     │
│       │  ┌─────────────────────────────────────────────────────────────┐   │
│       │  │  🎬 视频生成                                                 │   │
│       │  │                                                              │   │
│       │  │  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐    │   │
│       │  │  │ 📝 文生   │  │ 🖼️ 图生  │  │ 🔄 口型  │  │ 🎨 换背景│    │   │
│       │  │  │   视频    │  │   视频   │  │   同步   │  │          │    │   │
│       │  │  └──────────┘  └──────────┘  └──────────┘  └──────────┘    │   │
│       │  │                                                              │   │
│       │  └─────────────────────────────────────────────────────────────┘   │
│       │                                                                     │
│       │  ┌─────────────────────────────────────────────────────────────┐   │
│       │  │  📦 批量工具                             (更多功能开发中)    │   │
│       │  │                                                              │   │
│       │  │  ┌──────────┐  ┌──────────┐                                 │   │
│       │  │  │ 批量换脸 │  │ 批量背景 │   Coming Soon...                │   │
│       │  │  │          │  │          │                                 │   │
│       │  │  └──────────┘  └──────────┘                                 │   │
│       │  │                                                              │   │
│       │  └─────────────────────────────────────────────────────────────┘   │
│       │                                                                     │
└───────┴─────────────────────────────────────────────────────────────────────┘
```

### 7.3 数字人口播向导

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                        数字人口播                                    ✕     │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  ● 步骤 1    ○ 步骤 2    ○ 步骤 3    ○ 步骤 4                              │
│  上传音频    选择形象    选择背景    确认生成                              │
│                                                                             │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │                                                                     │   │
│  │                         📎 点击或拖拽上传                           │   │
│  │                                                                     │   │
│  │                    支持 MP3, WAV, M4A 格式                          │   │
│  │                       最长 5 分钟                                   │   │
│  │                                                                     │   │
│  │                      ─────── 或 ───────                             │   │
│  │                                                                     │   │
│  │                      🎙️ 点击录音                                    │   │
│  │                                                                     │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
│                                                                             │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                       [取消]  [下一步 →]   │
└─────────────────────────────────────────────────────────────────────────────┘

// 步骤 2: 选择形象
┌─────────────────────────────────────────────────────────────────────────────┐
│  选择数字人形象                                                             │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  预设形象                                                                   │
│  ┌────────┐ ┌────────┐ ┌────────┐ ┌────────┐ ┌────────┐                   │
│  │  ✓     │ │        │ │        │ │        │ │        │                   │
│  │ 商务男 │ │ 商务女 │ │ 时尚女 │ │ 阳光男 │ │ 甜美女 │                   │
│  └────────┘ └────────┘ └────────┘ └────────┘ └────────┘                   │
│                                                                             │
│  我的形象 (上传的口播视频模板)                         [+ 上传口播视频]     │
│  ┌────────┐ ┌────────┐                                                     │
│  │ 🎬     │ │   +    │                                                     │
│  │ 我的1  │ │  上传  │  ← 上传后即可使用，无需等待                          │
│  └────────┘ └────────┘                                                     │
│                                                                             │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                [← 上一步]  [下一步 →]       │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 7.4 Editor AI Tools 面板

```
┌────────────────────────────────────────────────────┐
│  AI 工具                                     ✕    │
├────────────────────────────────────────────────────┤
│                                                    │
│  已选中: Clip "口播片段-001"                        │
│  时长: 00:45  |  分辨率: 1920x1080                  │
│                                                    │
├────────────────────────────────────────────────────┤
│                                                    │
│  👤 人物处理                                        │
│                                                    │
│  ┌────────────────────────────────────────────┐   │
│  │  🎤 口型同步                                │   │
│  │  替换音频，重新同步口型                      │   │
│  │                                       →    │   │
│  └────────────────────────────────────────────┘   │
│                                                    │
│  ┌────────────────────────────────────────────┐   │
│  │  🔄 AI 换脸                                 │   │
│  │  换成其他人物形象                           │   │
│  │                                       →    │   │
│  └────────────────────────────────────────────┘   │
│                                                    │
│  ┌────────────────────────────────────────────┐   │
│  │  🎨 换背景                                  │   │
│  │  保留人物，更换背景                         │   │
│  │                                       →    │   │
│  └────────────────────────────────────────────┘   │
│                                                    │
├────────────────────────────────────────────────────┤
│                                                    │
│  🎨 画面处理                           即将推出    │
│                                                    │
│  ┌────────────────────────────────────────────┐   │
│  │  📈 画质增强                    Coming Soon │   │
│  └────────────────────────────────────────────┘   │
│                                                    │
└────────────────────────────────────────────────────┘
```

---

## 八、实现计划

### 8.1 开发阶段

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                          开发计划 / Implementation Plan                     │
└─────────────────────────────────────────────────────────────────────────────┘

Phase 1: 口型同步 MVP (Week 1-2) ⬅️ 当前阶段
├── [x] 可灵AI 服务层封装 (kling_ai_service.py)
├── [x] 可灵AI API 路由 (kling.py)
├── [ ] 数据库表结构 (ai_tasks)
├── [ ] Celery 异步任务 (lip_sync_task)
├── [ ] Rabbit Hole 口型同步页面
│   ├── [ ] 视频上传组件
│   ├── [ ] 音频上传组件
│   ├── [ ] 生成进度展示
│   └── [ ] 结果预览
├── [ ] 生成结果 → 素材库
└── [ ] API Key 配置 (留口子，后续测试)

Phase 2: 数字人形象库 (暂缓)
├── [ ] avatars 表结构
├── [ ] 预设形象数据
├── [ ] 形象选择器
├── [ ] 数字人口播向导
└── [ ] 用户自定义形象上传

Phase 3: Editor AI Tools
├── [ ] AIToolsPanel 扩展
├── [ ] Clip 右键 AI 菜单
├── [ ] 口型同步对话框
├── [ ] 处理结果替换 Clip
└── [ ] 换脸/换背景对话框

Phase 4: 更多创作工具
├── [ ] 产品动态展示
├── [ ] 文生视频 / 图生视频
├── [ ] AI 换脸
├── [ ] 换背景
└── [ ] 批量工具
```

### 8.2 技术依赖

| 组件 | 技术选型 | 说明 |
|------|---------|------|
| AI 服务 | 可灵AI API | 核心 AI 能力提供方 |
| 异步任务 | Celery + Redis | 长时间 AI 任务处理 |
| 实时通信 | WebSocket | 任务进度推送 |
| 存储 | Supabase Storage | 素材文件存储 |
| 数据库 | Supabase (PostgreSQL) | 任务、形象、素材元数据 |

### 8.3 环境配置

```env
# .env 新增配置

# 可灵AI API
KLING_API_KEY=your_api_key
KLING_API_SECRET=your_api_secret
KLING_API_BASE_URL=https://api.klingai.com/v1

# AI 任务配置
AI_TASK_POLL_INTERVAL=5        # 轮询间隔(秒)
AI_TASK_MAX_WAIT_TIME=600      # 最大等待时间(秒)
AI_TASK_RETRY_COUNT=3          # 失败重试次数
```

---

## 九、后续规划

### 9.1 计费模式 (与订阅系统一起开发)

```
┌─────────────────────────────────────────────────────────────────┐
│                        计费模式规划                              │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  免费用户:                                                       │
│  • 每月 X 次 AI 生成额度                                         │
│  • 仅限预设形象                                                  │
│  • 基础画质                                                      │
│                                                                 │
│  订阅用户:                                                       │
│  • 每月 XX 次 AI 生成额度                                        │
│  • 自定义形象上传                                                │
│  • 高清画质                                                      │
│  • 批量工具                                                      │
│                                                                 │
│  企业版:                                                         │
│  • 无限生成额度                                                  │
│  • 专属形象训练                                                  │
│  • API 接入                                                      │
│  • 优先处理队列                                                  │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 9.2 未来扩展

- **多语言口播**: 自动翻译 + 多语言口型同步
- **AI 脚本生成**: 输入主题 → 生成口播脚本
- **智能剪辑建议**: AI 分析口播内容，建议剪辑点
- **数据分析**: 不同形象/背景的播放效果对比

---

## 十、附录

### 10.1 可灵AI 能力对照表

| HoppingRabbit 功能 | 可灵AI 接口 | 说明 |
|------------------|------------|------|
| 数字人口播 | /lipsync/create | 音频 + 形象视频 → 口型同步 |
| 口型同步 | /lipsync/create | 视频 + 新音频 → 口型同步 |
| AI 换脸 | /faceswap/create | 视频 + 人脸图 → 换脸 |
| 换背景 | /segment/create | 视频 → 抠像 + 换背景 |
| 文生视频 | /video/text2video | 文本描述 → 视频 |
| 图生视频 | /video/image2video | 图片 → 动态视频 |

### 10.2 错误码定义

| 错误码 | 说明 | 处理方式 |
|--------|------|---------|
| KLING_AUTH_ERROR | API 认证失败 | 检查 API Key |
| KLING_QUOTA_EXCEEDED | 配额用尽 | 提示用户升级 |
| KLING_TASK_TIMEOUT | 任务超时 | 自动重试 |
| KLING_INVALID_INPUT | 输入不合法 | 提示用户检查 |
| ASSET_UPLOAD_FAILED | 素材上传失败 | 重试 |

### 10.3 预设数字人形象素材来源方案 (Phase 2 备用)

> **状态**: 暂缓实现，Phase 2 数字人形象库开发时参考

#### 免费素材库 (推荐起步)

| 来源 | 内容 | 授权 | 适合度 |
|------|------|------|--------|
| **Pixabay** | 773+ 免费 talking person 视频，4K/HD | Pixabay License（商用免费） | ⭐⭐⭐⭐ |
| **Pexels** | 167K+ talking person 视频 | Pexels License（商用免费） | ⭐⭐⭐⭐ |

**优点**: 免费、可商用、数量多  
**缺点**: 需要筛选适合口播场景的素材（正面、清晰唇部）

#### 自制预设素材 (最推荐)

| 方式 | 成本 | 时间 | 质量 |
|------|------|------|------|
| 团队拍摄 | 低（手机+灯光） | 1-2天 | ⭐⭐⭐⭐ |
| 雇佣模特 | ¥500-2000/人 | 1天 | ⭐⭐⭐⭐⭐ |
| 外包制作 | ¥2000-5000 | 3-5天 | ⭐⭐⭐⭐⭐ |

**口播模板拍摄要求:**
```
✓ 正面或略侧面（5-15度）
✓ 嘴唇清晰可见
✓ 稳定画面（三脚架）
✓ 时长 10-30 秒
✓ 纯色或简洁背景
✓ 1080p 以上分辨率
```

#### 付费素材平台

| 平台 | 价格 | 特点 |
|------|------|------|
| **Envato Elements** | $16.50/月 | 高质量商业素材 |
| **Shutterstock** | 按次/订阅 | 数量最多 |
| **iStock** | 按次购买 | Getty 旗下 |

#### MVP 阶段建议

```
Phase 2 数字人形象库:
├── 预设形象 x 5
│   ├── 1x 从 Pixabay/Pexels 精选（免费）
│   ├── 2x 团队自拍（低成本）
│   └── 2x 可灵官方（如有）
│
└── 用户上传
    └── 支持用户自己录制口播视频上传
```

---

**文档结束**

> 如有问题，请联系开发团队。
